import java.lang.Math;

public class circle {
    public static void main (String[] args) {
    //circle that calculates area, circumference and holds variable radius
    double Radius = 100.0f;
    double Area = Math.pow(Radius, 2);
    System.out.println(Area + "cm");
    }
}
